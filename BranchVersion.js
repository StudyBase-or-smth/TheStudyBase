// Auto-generated alongside BranchVersion.json by scripts/update-branch-date.js.
// Loaded as a plain <script> (not fetched) so the header's branch/version
// badge also works when index.html is opened directly via file:// — Chrome
// blocks fetch()/XHR of local files under file://, which is why the badge
// previously always fell back to the hardcoded "dev · Local" placeholder
// during local testing. Keep this in sync with BranchVersion.json; the
// update script regenerates both together.
window.BRANCH_VERSION = {
  "date": "21-7-26",
  "branch": "Devpanel",
  "version": "6.0.5",
  "colour": "main"
};
