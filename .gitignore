# Environment variables — never commit real API keys
.env
.env.local
.env.*.local

# Netlify
.netlify/

# Local-only helper scripts; keep the branch-badge updater tracked
scripts/*
!scripts/update-branch-date.js

# Node
node_modules/

# OS / editor junk
.DS_Store
Thumbs.db
*.log

# Design source file — not needed at runtime, only icon.png is served
icon.af
Blank.html


