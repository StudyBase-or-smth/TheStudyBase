// grade.js (repo root) — UNUSED, safe to delete.
//
// This is a stale duplicate of netlify/functions/grade.js. netlify.toml sets
// `functions = "netlify/functions"`, so Netlify only ever deploys the copy in
// that folder — this root-level file isn't wired to anything and doesn't run.
// It's left as a harmless stub instead of real duplicated logic so nobody
// mistakenly edits this copy and wonders why their change has no effect.
//
// I can't delete files from here (this session only has read/write access,
// not delete, on your machine) — feel free to `git rm grade.js` next time
// you're in the repo.
